namespace RenameWizard
{
    public enum TextStyle
    {
        KeepIt,
        CapitalLetter,
        TitleCase,
        UpperCase,
        LowerCase
    }

    public enum ExtensionStyle
    {
        UpperCase,
        LowerCase
    }

    public enum NumerationType
    {
        Simple,
        Smart,
        FindInFile,
    }

    public enum Order
    {
        TextNumber,
        NumberText,
    }
}
